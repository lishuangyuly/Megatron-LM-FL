"""ActivationGroup primitives (commit 3 placeholder).

Will host ``ActivationGroup`` (sort / budget / bucket + prologue / issue /
epilogue for both offload and onload) and ``CopyTaskGroup`` (round-robin
bucket assignment across ``stages`` copy buckets).

Empty in commit 1.
"""
